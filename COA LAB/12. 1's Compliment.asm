DATA SEGMENT
    A DB 05H
    RES DB ?
DATA ENDS

CODE SEGMENT 
    ASSUME CS:CODE, DS:DATA
    
START:
    MOV AX, DATA
    MOV DS, AX
    MOV AL, A
    NOT AL
    MOV RES, AL
    CODE ENDS
END START